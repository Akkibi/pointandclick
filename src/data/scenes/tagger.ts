import type { SceneType } from "../../types/scene";

const tagger: SceneType = {
  isStageLookingFront: true,
  doors: {
    front: {
      "#fe0000": "scene35", // scène suivante
    },
  },
  sound: "path/of/sound",
};

export default tagger;